# ⚡ Установка одной командой

## 🚀 Самый быстрый способ

### Вариант 1: Из GitHub (одна команда!)

```bash
ssh root@ВАШ_IP

# Установка одной командой с вашего GitHub:
GITHUB_REPO="https://github.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ.git" \
curl -fsSL https://raw.githubusercontent.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ/main/auto_deploy.sh | sudo bash
```

**Готово!** Через 3-5 минут откройте: `http://ВАШ_IP`

---

### Вариант 2: Локальные файлы (если уже клонировали)

```bash
ssh root@ВАШ_IP
cd /opt/shop-deploy
sudo ./auto_deploy.sh
```

---

## 🎯 Что происходит автоматически?

✅ Устанавливаются все пакеты (PostgreSQL, Nginx, Node.js, Python)  
✅ Создаётся пользователь `shopapp`  
✅ Создаётся база данных с автоматическим паролем  
✅ Клонируется код из GitHub  
✅ Собирается фронтенд  
✅ Настраивается systemd сервис  
✅ Настраивается Nginx  
✅ Запускается приложение  

**Никаких вопросов - всё автоматически!**

---

## ⚙️ Настройка параметров (опционально)

Можете указать свои параметры через переменные окружения:

```bash
# Пример с кастомными параметрами:
GITHUB_REPO="https://github.com/user/repo.git" \
GIT_BRANCH="main" \
APP_USER="myshop" \
DB_NAME="myshop_db" \
DB_USER="myshop_user" \
DB_PASSWORD="my_secure_password" \
APP_PORT="5000" \
curl -fsSL https://raw.githubusercontent.com/user/repo/main/auto_deploy.sh | sudo bash
```

### Доступные параметры:

| Параметр | По умолчанию | Описание |
|----------|--------------|----------|
| `GITHUB_REPO` | (пусто) | URL GitHub репозитория |
| `GIT_BRANCH` | `main` | Ветка для клонирования |
| `APP_USER` | `shopapp` | Имя пользователя приложения |
| `DB_NAME` | `shop_db` | Имя базы данных |
| `DB_USER` | `shop_user` | Пользователь БД |
| `DB_PASSWORD` | (авто) | Пароль БД (генерируется автоматически) |
| `APP_PORT` | `5000` | Порт приложения |
| `LOAD_SEED_DATA` | (нет) | `yes` для загрузки тестовых данных |

---

## 📦 С тестовыми данными

```bash
GITHUB_REPO="https://github.com/user/repo.git" \
LOAD_SEED_DATA="yes" \
curl -fsSL https://raw.githubusercontent.com/user/repo/main/auto_deploy.sh | sudo bash
```

---

## 🔄 Обновление приложения

После изменений в GitHub:

```bash
ssh root@ВАШ_IP
cd /home/shopapp/app
sudo -u shopapp git pull
sudo ./update_vps.sh
```

---

## 🔐 Приватный репозиторий?

### Настройте SSH ключ перед установкой:

```bash
ssh root@ВАШ_IP

# Создайте ключ
ssh-keygen -t ed25519 -C "vps"

# Покажите ключ
cat ~/.ssh/id_ed25519.pub

# Добавьте в GitHub: Settings → SSH keys → New SSH key
```

Используйте SSH URL:
```bash
GITHUB_REPO="git@github.com:user/repo.git" \
curl -fsSL https://raw.githubusercontent.com/user/repo/main/auto_deploy.sh | sudo bash
```

---

## 🛠️ Полезные команды после установки

```bash
# Статус
systemctl status shop-app

# Логи
journalctl -u shop-app -f

# Рестарт
systemctl restart shop-app

# Посмотреть пароль БД
cat /home/shopapp/app/.env

# Резервная копия
cd /home/shopapp/app && sudo ./backup_db.sh

# Исправить права (если 403)
sudo ./fix_permissions.sh
```

---

## 🆘 Проблемы?

### Не клонируется репозиторий
Проверьте URL и доступ к GitHub

### Белая страница
```bash
cd /home/shopapp/app
sudo ./fix_permissions.sh
```

### Логи установки
Скрипт выводит все шаги в консоль

---

## ✨ Сравнение способов установки

| Способ | Команд | Время | Вопросы |
|--------|--------|-------|---------|
| **auto_deploy.sh** | 1 | 3-5 мин | Нет ❌ |
| deploy_vps.sh | 1 | 5-7 мин | Да ✅ |
| Вручную | 20+ | 30+ мин | Много |

**Используйте `auto_deploy.sh` для максимальной скорости!** ⚡

---

## 📚 Дополнительная документация

- **Интерактивная установка**: `QUICK_START_RU.md`
- **Настройка домена**: `DOMAIN_SETUP_RU.md`
- **Все скрипты**: `VPS_SCRIPTS_README.md`

---

**Установка за 1 команду! Просто и быстро! 🚀**
