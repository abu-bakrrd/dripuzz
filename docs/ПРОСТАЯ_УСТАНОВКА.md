# Быстрая установка на VPS

## Интерактивная установка

Запустите интерактивный скрипт, который задаст вам вопросы:

### Шаг 1: Подключитесь к VPS
```bash
ssh root@ВАШ_IP
```

### Шаг 2: Загрузите и запустите скрипт деплоя

```bash
wget https://raw.githubusercontent.com/ВАШ_ПОЛЬЗОВАТЕЛЬ/ВАШ_РЕПО/main/deploy_vps.sh
chmod +x deploy_vps.sh
sudo ./deploy_vps.sh
```

Скрипт спросит:
1. URL вашего GitHub репозитория
2. Имя пользователя для приложения
3. Пароль для базы данных
4. Ваш домен (опционально)

---

## После установки

### Ваш сайт доступен:
- **Без домена**: `http://ВАШ_IP`
- **С доменом**: `http://ВАШ_ДОМЕН`

### Войдите в админку:
1. Перейдите на `http://ВАШ_IP/admin`
2. Зарегистрируйтесь — станете первым администратором
3. Добавьте категории и товары

### Полезные команды:
```bash
# Статус приложения
systemctl status shop-app

# Просмотр логов
journalctl -u shop-app -f

# Перезапуск
systemctl restart shop-app

# Исправить белую страницу / 403
cd /home/shopapp/app && sudo ./fix_permissions.sh
```

---

## SSL сертификат (HTTPS)

```bash
cd /home/shopapp/app
sudo ./setup_ssl.sh
```

Или вручную:
```bash
sudo apt install certbot python3-certbot-nginx -y
sudo certbot --nginx -d ВАШ_ДОМЕН
```

---

## Обновление кода

```bash
cd /home/shopapp/app
sudo -u shopapp git pull
sudo ./update_vps.sh
```

---

## Часто задаваемые вопросы

**Q: Как добавить товары?**  
A: Через админ-панель `/admin` или напрямую в БД:
```bash
sudo -u postgres psql shop_db
INSERT INTO products (name, description, price, images, category_id) 
VALUES ('Товар', 'Описание', 1000, ARRAY['url.jpg'], 'category-id');
```

**Q: Как изменить настройки магазина?**  
A: Отредактируйте файл `/home/shopapp/app/config/settings.json`

**Q: Как настроить платежи?**  
A: Через админ-панель `/admin` → Настройки или в `config/settings.json`

---

**Готово! Ваш магазин работает! 🎉**

Подробнее: [docs/ПОЛНОЕ_РУКОВОДСТВО.md](docs/ПОЛНОЕ_РУКОВОДСТВО.md)
