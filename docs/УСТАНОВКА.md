# 📦 Установка на VPS

## Два способа установки

### ⚡ Способ 1: Напрямую из GitHub (Рекомендуется)

**Самый быстрый способ!** Скрипт сам скачает код из вашего репозитория.

#### Шаг 1: Подключитесь к VPS

```bash
ssh root@ВАШ_IP
```

#### Шаг 2: Скачайте и запустите скрипт установки

```bash
wget https://raw.githubusercontent.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ/main/deploy_vps.sh
chmod +x deploy_vps.sh
sudo ./deploy_vps.sh
```

#### Шаг 3: Введите данные

Когда скрипт попросит, введите:

```
URL GitHub репозитория: https://github.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ.git
Ветка [main]: main
Пароль для БД: ваш_надёжный_пароль
```

Остальные параметры можно оставить по умолчанию (просто нажимайте Enter).

**Готово!** ✅

Откройте в браузере: `http://ВАШ_IP`

---

### 📁 Способ 2: Сначала клонировать, потом установить

Если хотите сначала посмотреть код:

```bash
# 1. Клонируем репозиторий
git clone https://github.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ.git /opt/shop-deploy
cd /opt/shop-deploy

# 2. Запускаем установку
chmod +x deploy_vps.sh
sudo ./deploy_vps.sh
```

Когда скрипт спросит про GitHub репозиторий - **оставьте пустым** (просто нажмите Enter).
Скрипт использует уже скачанные файлы.

---

## 🔐 Приватный репозиторий?

### SSH ключ (проще всего)

```bash
# На VPS создайте ключ
ssh-keygen -t ed25519 -C "vps"

# Покажите ключ
cat ~/.ssh/id_ed25519.pub
```

Скопируйте ключ и добавьте в GitHub:
- GitHub → Settings → SSH and GPG keys → New SSH key

Используйте SSH URL:
```
git@github.com:ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ.git
```

### Personal Access Token

1. Создайте токен: GitHub → Settings → Developer settings → Personal access tokens
2. Используйте URL с токеном:
```
https://ВАШ_ТОКЕН@github.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ.git
```

---

## 🔄 Обновление приложения

После изменений в коде:

```bash
# На вашем компьютере
git add .
git commit -m "Описание изменений"
git push

# На VPS
ssh root@ВАШ_IP
cd /home/shopapp/app
sudo -u shopapp git pull
sudo ./update_vps.sh
```

---

## 🛠️ Полезные команды

```bash
# Статус приложения
systemctl status shop-app

# Логи в реальном времени
journalctl -u shop-app -f

# Перезапуск
systemctl restart shop-app

# Резервная копия БД
cd /home/shopapp/app
sudo ./backup_db.sh

# Исправить ошибки 403
sudo ./fix_permissions.sh

# Домен и SSL
sudo ./setup_domain.sh
sudo ./setup_ssl.sh
```

---

## 📚 Дополнительные инструкции

- **Быстрый старт на GitHub**: `GITHUB_QUICK_START.md` ⭐
- **Подробная инструкция**: `QUICK_START_RU.md`
- **Настройка домена**: `DOMAIN_SETUP_RU.md`
- **Все скрипты**: `VPS_SCRIPTS_README.md`

---

## ❓ Частые вопросы

### В чём разница между способами?

**Способ 1** (из GitHub):
- ✅ Быстрее всего
- ✅ Код всегда актуальный
- ✅ Легко обновлять через `git pull`
- ❌ Нужен доступ к GitHub с VPS

**Способ 2** (клонировать сначала):
- ✅ Можете посмотреть код перед установкой
- ✅ Работает одинаково
- ✅ Тоже поддерживает обновления
- ℹ️ Просто дополнительный шаг

### Какой способ лучше?

**Способ 1** - если вы доверяете своему коду и хотите быстро развернуть.
**Способ 2** - если хотите сначала проверить или изменить что-то в коде.

Оба способа работают одинаково хорошо!

---

## 🎯 Что делает скрипт?

Скрипт `deploy_vps.sh` автоматически:

1. ✅ Устанавливает PostgreSQL
2. ✅ Устанавливает Nginx
3. ✅ Устанавливает Node.js и Python
4. ✅ Клонирует код из GitHub (или использует локальные файлы)
5. ✅ Создаёт базу данных
6. ✅ Собирает фронтенд
7. ✅ Настраивает systemd сервис
8. ✅ Настраивает Nginx
9. ✅ Запускает приложение

**Весь процесс занимает 3-5 минут!** ⚡

---

## 🆘 Проблемы?

### Не клонируется репозиторий
```bash
# Проверьте доступ к GitHub
ping github.com

# Проверьте URL репозитория
```

### Белая страница
```bash
cd /home/shopapp/app
sudo ./fix_permissions.sh
```

### Приложение не запускается
```bash
# Посмотрите логи
journalctl -u shop-app -n 100
```

**Нужна помощь?** Смотрите `VPS_DEPLOY_GUIDE.md`

---

**Успешного развёртывания! 🚀**
