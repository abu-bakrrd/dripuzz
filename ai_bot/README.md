# 🤖 AI Customer Support Bot

Telegram бот с искусственным интеллектом для ответов клиентам на вопросы о товарах.

---

## ⚡ Быстрый старт

### Шаг 1: Получить токены

#### 1.1 Создать Telegram бота

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Введите название: "Ваш Магазин - Помощник"
4. Введите username: `your_shop_ai_bot`
5. **Скопируйте токен** (например: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

#### 1.2 Получить Gemini API ключ

1. Перейдите на [aistudio.google.com/apikey](https://aistudio.google.com/apikey)
2. Войдите через Google аккаунт
3. Нажмите **"Create API Key"**
4. **Скопируйте ключ** (например: `AIzaSyABC123...`)

**Бесплатно**: 60 запросов в минуту навсегда! 🎉

---

### Шаг 2: Настроить .env файл

Создайте файл `.env` в **корне проекта** (не в папке ai_bot):

```env
# AI Customer Support Bot
AI_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
GEMINI_API_KEY=AIzaSyABC123...

# Database (используется та же БД что и магазин)
DATABASE_URL=postgresql://shop_user:your_password@localhost:5432/shop_db
```

---

### Шаг 3: Установить зависимости

```bash
pip install google-generativeai
```

Или все сразу:

```bash
pip install -r requirements.txt
```

---

### Шаг 4: Добавить тестовые товары (для локального тестирования)

```bash
cd ai_bot
python test_data.py
```

Это добавит в БД:

- ✅ 4 тестовых товара (футболка, кроссовки, джинсы, куртка)
- ✅ Данные о наличии (цвета, размеры, количество)

---

### Шаг 5: Запустить бота

**Windows:**

```bash
cd ai_bot
start_ai_bot.bat
```

Или:

```bash
cd ai_bot
python ai_customer_bot.py
```

**Linux/Mac:**

```bash
cd ai_bot
chmod +x start_ai_bot.sh
./start_ai_bot.sh
```

Или:

```bash
cd ai_bot
python3 ai_customer_bot.py
```

---

## 🧪 Тестирование

### 1. Найти бота в Telegram

Найдите вашего бота по username (например: `@your_shop_ai_bot`)

### 2. Отправить /start

Должно прийти приветствие от бота

### 3. Задать вопросы

Примеры:

- "Какие товары у вас есть?"
- "Покажи футболки"
- "Есть ли черная футболка размера M?"
- "Сколько стоит Nike Pro?"
- "Какие размеры кроссовок в наличии?"

### 4. Проверить ответы

AI должен отвечать на основе данных из БД с эмодзи и красивым форматированием.

---

## 🚀 Запуск на VPS (24/7)

### Вариант 1: systemd service (рекомендуется)

Создайте файл `/etc/systemd/system/ai-customer-bot.service`:

```ini
[Unit]
Description=AI Customer Support Bot
After=network.target postgresql.service

[Service]
Type=simple
User=shopapp
WorkingDirectory=/home/shopapp/app/ai_bot
Environment="PATH=/usr/bin:/usr/local/bin"
EnvironmentFile=/home/shopapp/app/.env
ExecStart=/usr/bin/python3 ai_customer_bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Запустите:

```bash
sudo systemctl daemon-reload
sudo systemctl enable ai-customer-bot
sudo systemctl start ai-customer-bot
```

Проверка статуса:

```bash
sudo systemctl status ai-customer-bot
```

Логи:

```bash
sudo journalctl -u ai-customer-bot -f
```

### Вариант 2: screen/tmux

```bash
cd ai_bot
screen -S aibot
python3 ai_customer_bot.py
# Нажать Ctrl+A, затем D для отсоединения
```

Вернуться к боту:

```bash
screen -r aibot
```

---

## 📊 Примеры диалогов

### Вопрос о товарах:

```
👤 Клиент: Какие товары у вас есть?

🤖 Бот:
🛍 У нас в наличии:

1️⃣ Футболка Nike Pro - 150,000 сум
   🎨 Цвета: черный, белый, синий
   📏 Размеры: S, M, L, XL

2️⃣ Кроссовки Adidas Ultraboost - 450,000 сум
   🎨 Цвета: белый, черный
   📏 Размеры: 40, 41, 42, 43, 44

Чем могу помочь?
```

### Вопрос о наличии:

```
👤 Клиент: Есть ли черная футболка размера M?

🤖 Бот:
✅ Да! Футболка Nike Pro в черном цвете, размер M есть в наличии (3 шт).

Цена: 150,000 сум

Хотите оформить заказ? Свяжитесь с нашим менеджером!
```

---

## 📁 Структура папки ai_bot

```
ai_bot/
├── ai_customer_bot.py      # Основной файл бота
├── ai_db_helper.py          # Работа с БД
├── test_data.py             # Тестовые товары
├── start_ai_bot.bat         # Запуск (Windows)
├── start_ai_bot.sh          # Запуск (Linux/Mac)
├── .env.example             # Пример конфигурации
└── README.md                # Эта инструкция
```

---

## ❓ Частые проблемы

### "AI_BOT_TOKEN не найден"

Проверьте файл `.env` в **корне проекта** (не в ai_bot) - токен должен быть указан без кавычек

### "GEMINI_API_KEY не найден"

Получите ключ на [aistudio.google.com/apikey](https://aistudio.google.com/apikey)

### "Ошибка подключения к БД"

Проверьте `DATABASE_URL` в `.env`

### "Бот не отвечает"

1. Проверьте, запущен ли бот
2. Проверьте токен бота через @BotFather

### "AI отвечает 'нет товаров'"

Добавьте тестовые данные: `python test_data.py`

---

**Готово! Ваш AI помощник работает! 🎉**
