# 🚀 Быстрая установка из GitHub

## Развёртывание за 5 минут! ⚡

### Что нужно:
- VPS с Ubuntu 22.04
- IP адрес вашего VPS
- URL вашего GitHub репозитория

---

## 📋 Инструкция

### Шаг 1: Подключитесь к VPS

```bash
ssh root@ВАШ_IP_АДРЕС
```

### Шаг 2: Запустите одну команду!

```bash
curl -fsSL https://raw.githubusercontent.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ/main/deploy_vps.sh | sudo bash
```

**ИЛИ** вручную:

```bash
# Скачайте скрипт установки
wget https://raw.githubusercontent.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ/main/deploy_vps.sh

# Запустите
chmod +x deploy_vps.sh
sudo ./deploy_vps.sh
```

### Шаг 3: Введите данные

Скрипт спросит вас:

1. **URL GitHub репозитория**: 
   ```
   https://github.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ.git
   ```
   
2. **Ветка** (нажмите Enter для `main`):
   ```
   main
   ```

3. **Пароль для базы данных** (придумайте надёжный)

4. **Остальное** - просто нажимайте Enter (используются значения по умолчанию)

### ✅ Готово!

Через 3-5 минут откройте в браузере:
```
http://ВАШ_IP_АДРЕС
```

---

## 🔄 Обновление приложения

Когда внесёте изменения в GitHub:

```bash
# 1. На вашем компьютере - отправьте изменения в GitHub
git add .
git commit -m "Описание изменений"
git push

# 2. На VPS - получите обновления
ssh root@ВАШ_IP_АДРЕС
cd /home/shopapp/app
sudo -u shopapp git pull
sudo ./update_vps.sh
```

---

## 🔐 Приватный репозиторий?

### Вариант 1: SSH ключ (проще)

```bash
# На VPS создайте SSH ключ
ssh-keygen -t ed25519 -C "vps-deploy"

# Покажите ключ
cat ~/.ssh/id_ed25519.pub

# Скопируйте ключ и добавьте в GitHub:
# GitHub → Settings → SSH and GPG keys → New SSH key
```

Используйте SSH URL при установке:
```
git@github.com:ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ.git
```

### Вариант 2: Personal Access Token

```bash
# Создайте токен в GitHub:
# Settings → Developer settings → Personal access tokens → Generate new token

# Используйте URL с токеном:
https://ВАШ_ТОКЕН@github.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ.git
```

---

## 🛠️ Полезные команды

```bash
# Проверить статус приложения
systemctl status shop-app

# Посмотреть логи
journalctl -u shop-app -f

# Перезапустить
systemctl restart shop-app

# Резервная копия БД
cd /home/shopapp/app
sudo ./backup_db.sh

# Исправить права (если ошибки 403)
sudo ./fix_permissions.sh

# Подключить домен
sudo ./setup_domain.sh

# Установить SSL
sudo ./setup_ssl.sh
```

---

## 🆘 Проблемы?

### Не клонируется репозиторий
- Проверьте URL репозитория
- Для приватного репозитория используйте SSH ключ или токен
- Проверьте доступ к интернету на VPS: `ping github.com`

### Белая страница / 403 ошибки
```bash
cd /home/shopapp/app
sudo ./fix_permissions.sh
```

### Приложение не запускается
```bash
# Посмотрите логи
journalctl -u shop-app -n 100

# Проверьте БД
systemctl status postgresql
```

---

## 📚 Подробная документация

- **Развёртывание из GitHub**: `DEPLOY_FROM_GITHUB.md`
- **Настройка домена и SSL**: `DOMAIN_SETUP_RU.md`
- **Все скрипты**: `VPS_SCRIPTS_README.md`
- **Полное руководство**: `VPS_DEPLOY_GUIDE.md`

---

## ✨ Что делает скрипт автоматически?

✅ Устанавливает все необходимые пакеты (PostgreSQL, Nginx, Node.js, Python)  
✅ Клонирует ваш репозиторий из GitHub  
✅ Создаёт и настраивает базу данных  
✅ Собирает фронтенд приложения  
✅ Настраивает systemd сервис  
✅ Настраивает Nginx  
✅ Настраивает Firewall  
✅ Запускает приложение  

**Всё готово к работе!** 🎉

---

## 📌 Примечания

- Скрипт безопасно перезапустить - он проверяет существующие настройки
- Все пароли сохраняются в файле `.env`
- Логи находятся в: `/var/log/nginx/` и `journalctl -u shop-app`
- Резервные копии БД создаются автоматически при обновлении

---

**Развёртывание занимает 3-5 минут! ⚡**
