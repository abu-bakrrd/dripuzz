# 📚 Полное руководство - Telegram Mini App Shop

## Содержание

1. [О шаблоне](#о-шаблоне)
2. [Кастомизация магазина](#кастомизация-магазина)
3. [Развертывание на VPS](#развертывание-на-vps)
4. [Обновление приложения](#обновление-приложения)
5. [Настройка домена и SSL](#настройка-домена-и-ssl)
6. [Админ-панель](#админ-панель)
7. [Платежные системы](#платежные-системы)
8. [Telegram уведомления](#telegram-уведомления)
9. [Email и сброс пароля](#email-и-сброс-пароля)
10. [Решение проблем](#решение-проблем)

---

# О шаблоне

Это универсальный шаблон для создания магазина в Telegram Mini App. Весь магазин настраивается через JSON файлы - не нужно изменять код!

## Что можно настроить:

- ✅ Название магазина и описание
- ✅ Логотип
- ✅ Цветовую схему (все цвета)
- ✅ Валюту (символ, код, позицию)
- ✅ Контакт менеджера в Telegram
- ✅ Тексты интерфейса
- ✅ Шрифты и их начертание
- ✅ Размер логотипа

## Что находится в базе данных:

- Категории товаров
- Товары (название, описание, цена, фото, цвета, характеристики)
- Пользователи
- Корзины и избранное
- Все токены ботов и API ключи (через .env)

---

# Кастомизация магазина

## Шаг 1: Настройка основных параметров

### Откройте файл `config/settings.json`

Это главный файл конфигурации. Вот что в нём можно изменить:

```json
{
	"shopName": "Ваш Магазин",
	"description": "Описание вашего магазина",
	"logo": "/config/logo.svg",

	"currency": {
		"symbol": "₽",
		"code": "RUB",
		"position": "after"
	},

	"managerContact": "@ваш_телеграм",

	"colorScheme": {
		"background": "#FEFEFE",
		"foreground": "#1A1A1A",
		"card": "#FFFFFF",
		"cardForeground": "#1A1A1A",
		"popover": "#FFFFFF",
		"popoverForeground": "#1A1A1A",
		"primary": "#EADCF0",
		"primaryForeground": "#1A1A1A",
		"secondary": "#F5F5F5",
		"secondaryForeground": "#1A1A1A",
		"muted": "#F5F5F5",
		"mutedForeground": "#737373",
		"accent": "#F5F5F5",
		"accentForeground": "#1A1A1A",
		"destructive": "#FF4444",
		"destructiveForeground": "#FFFFFF",
		"border": "#E5E5E5",
		"input": "#E5E5E5",
		"ring": "#1A1A1A"
	},

	"sortOptions": [
		{ "id": "new", "label": "Новинки", "emoji": "✨" },
		{ "id": "popular", "label": "Популярное", "emoji": "🔥" },
		{ "id": "price_low", "label": "Дешевле", "emoji": "💰" },
		{ "id": "price_high", "label": "Дороже", "emoji": "💎" }
	],

	"ui": {
		"maxWidth": "420px",
		"productsPerPage": 12,
		"showCategoryIcons": true,
		"showPriceFilter": true
	},

	"texts": {
		"addToCart": "В корзину",
		"checkout": "Оформить заказ",
		"cart": "Корзина",
		"favorites": "Избранное",
		"emptyCart": "Корзина пуста",
		"emptyFavorites": "Нет избранных товаров"
	},

	"fonts": {
		"fontFamily": "Inter",
		"fontFile": null,
		"productName": { "weight": 500 },
		"price": { "weight": 600 },
		"description": { "weight": 400 }
	},

	"logoSize": 32
}
```

### Параметры подробно:

#### Основные настройки:

- **shopName** - название магазина (отображается в шапке)
- **description** - описание (для SEO)
- **logo** - путь к логотипу (обычно `/config/logo.svg`)

#### Валюта:

- **symbol** - символ валюты (`₽`, `$`, `€`, `₸`, и т.д.)
- **code** - код валюты (`RUB`, `USD`, `EUR`, `KZT`)
- **position** - позиция символа: `"after"` (1000₽) или `"before"` ($1000)

#### Контакт:

- **managerContact** - ваш Telegram для связи (например: `@username`)

#### Цветовая схема:

Все цвета в формате HEX (#RRGGBB):

- **background** - фон страницы
- **foreground** - основной цвет текста
- **primary** - основной акцентный цвет
- **card** - фон карточек
- **border** - цвет границ
- И т.д.

**Примеры цветовых схем:**

Пастельный розовый (по умолчанию):

```json
"primary": "#EADCF0",
"primaryForeground": "#1A1A1A"
```

Голубой минимализм:

```json
"primary": "#E3F2FD",
"primaryForeground": "#1A1A1A"
```

Тёмный модерн:

```json
"primary": "#2C3E50",
"primaryForeground": "#FFFFFF"
```

#### Опции сортировки:

Можете изменить текст и эмодзи:

```json
{ "id": "new", "label": "Новое", "emoji": "🆕" }
```

#### UI настройки:

- **maxWidth** - максимальная ширина интерфейса (`420px` для телефонов)
- **productsPerPage** - товаров на странице
- **showCategoryIcons** - показывать ли иконки категорий
- **showPriceFilter** - показывать ли фильтр по цене

#### Тексты:

Все надписи кнопок можно изменить

#### Шрифты:

- **fontFamily** - название шрифта (`Inter`, `Roboto`, `Open Sans`)
- **fontFile** - путь к файлу шрифта (если свой шрифт)
- **productName/price/description** - толщина шрифта (400-700)

#### Размер логотипа:

- **logoSize** - размер в пикселях (рекомендуется 32-48)

---

## Шаг 2: Замена логотипа

### Подготовка логотипа:

1. **Формат**: SVG (рекомендуется) или PNG
2. **Размер**: 200x200px или квадратный
3. **Прозрачный фон** (для PNG)

### Замена:

1. Положите ваш логотип в папку `config/`
2. Назовите его `logo.svg` или `logo.png`
3. Если другое имя - укажите в settings.json:
   ```json
   "logo": "/config/мой-логотип.svg"
   ```

---

## Шаг 3: Добавление товаров и категорий

Товары и категории хранятся в базе данных PostgreSQL.

### На VPS после установки:

#### Вариант A: Через SQL

```bash
# Подключитесь к VPS
ssh root@ВАШ_IP

# Подключитесь к базе данных
sudo -u postgres psql shop_db

# Добавьте категорию
INSERT INTO categories (id, name, icon)
VALUES (gen_random_uuid(), 'Электроника', '💻');

# Посмотрите ID категории
SELECT id, name FROM categories;

# Добавьте товар (замените CATEGORY_ID на ID из предыдущей команды)
INSERT INTO products (id, name, description, price, images, category_id)
VALUES (
  gen_random_uuid(),
  'iPhone 15 Pro',
  'Флагманский смартфон Apple',
  89990,
  ARRAY['https://example.com/iphone.jpg'],
  'CATEGORY_ID'
);

# Выход
\q
```

#### Вариант Б: Через Python скрипт

Создайте файл `add_products.py`:

```python
import psycopg2
import os

# Подключение к БД
conn = psycopg2.connect(os.environ['DATABASE_URL'])
cur = conn.cursor()

# Добавить категорию
cur.execute("""
    INSERT INTO categories (name, icon)
    VALUES (%s, %s)
    RETURNING id
""", ('Электроника', '💻'))
category_id = cur.fetchone()[0]

# Добавить товары
products = [
    ('iPhone 15 Pro', 'Флагманский смартфон', 89990, ['https://example.com/iphone.jpg']),
    ('MacBook Pro', 'Мощный ноутбук', 199990, ['https://example.com/macbook.jpg']),
]

for name, desc, price, images in products:
    cur.execute("""
        INSERT INTO products (name, description, price, images, category_id)
        VALUES (%s, %s, %s, %s, %s)
    """, (name, desc, price, images, category_id))

conn.commit()
cur.close()
conn.close()

print("Товары добавлены!")
```

Запустите:

```bash
cd /home/shopapp/app
source venv/bin/activate
python3 add_products.py
```

#### Вариант В: Используйте готовый seed_db.py

```bash
# На VPS
cd /home/shopapp/app
source venv/bin/activate
python3 seed_db.py
```

Это загрузит тестовые данные. Потом можете их изменить или удалить.

---

## Шаг 4: Тестирование локально (опционально)

Если хотите протестировать перед развертыванием:

```bash
# На вашем компьютере
npm install
npm run dev

# В другом терминале
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или venv\Scripts\activate  # Windows
pip install -r requirements.txt
python app.py
```

Откройте: http://localhost:5000

---

# Развертывание на VPS

## Подготовка

### Что вам нужно:

1. **VPS сервер** с Ubuntu 22.04
   - Минимум: 1GB RAM, 1 CPU, 10GB диск
   - Рекомендуется: 2GB RAM, 2 CPU, 20GB диск
2. **IP адрес** вашего VPS

3. **SSH доступ** с правами root

4. **GitHub репозиторий** с вашим кодом (или локальные файлы)

---

### Способ 1: Мастер-установка (РЕКОМЕНДУЕТСЯ) 🔥

Это самый полный способ - одна команда установит сайт, базу, SSL и обоих ботов!

#### Шаг 1: Подключитесь к VPS

```bash
ssh root@ВАШ_IP_АДРЕС
```

#### Шаг 2: Запустите мастер-скрипт

```bash
git clone https://github.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ.git app
cd app/scripts
chmod +x master_deploy.sh
sudo ./master_deploy.sh
```

**Время: 5-10 минут**

### Шаг 3: Откройте сайт

После завершения откройте в браузере:

```
http://ВАШ_IP_АДРЕС
```

**Готово!** 🎉

---

## Способ 2: Интерактивная установка

Если хотите настроить параметры вручную:

### Шаг 1: Подключитесь к VPS

```bash
ssh root@ВАШ_IP_АДРЕС
```

#### Шаг 3: Запустите установку

```bash
sudo ./scripts/master_deploy.sh
```

### Скрипт спросит:

1. **URL GitHub репозитория** (или Enter для локальных файлов):

   ```
   https://github.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ.git
   ```

2. **Ветка** [main]:

   ```
   main
   ```

   (или просто Enter)

3. **Имя пользователя** [shopapp]:

   ```
   shopapp
   ```

   (или просто Enter)

4. **Имя базы данных** [shop_db]:

   ```
   shop_db
   ```

   (или просто Enter)

5. **Пользователь БД** [shop_user]:

   ```
   shop_user
   ```

   (или просто Enter)

6. **Пароль для БД**:

   ```
   ваш_надёжный_пароль
   ```

   (придумайте безопасный пароль)

7. **Порт** [5000]:

   ```
   5000
   ```

   (или просто Enter)

8. **Загрузить тестовые данные?** (y/n):
   ```
   y
   ```
   (если хотите примеры товаров)

### Шаг 4: Дождитесь завершения

Установка займёт 5-7 минут.

### Шаг 5: Откройте сайт

```
http://ВАШ_IP_АДРЕС
```

---

## Приватный GitHub репозиторий

Если ваш репозиторий приватный, нужно настроить доступ:

### Вариант A: SSH ключ (рекомендуется)

```bash
# На VPS создайте SSH ключ
ssh-keygen -t ed25519 -C "vps-deploy"

# Нажмите Enter 3 раза (без пароля)

# Покажите публичный ключ
cat ~/.ssh/id_ed25519.pub
```

**Скопируйте весь вывод** и добавьте в GitHub:

1. Откройте GitHub → Settings
2. SSH and GPG keys
3. New SSH key
4. Вставьте ключ
5. Save

Теперь используйте SSH URL:

```bash
GITHUB_REPO="git@github.com:ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ.git" \
curl -fsSL https://... | sudo bash
```

### Вариант Б: Personal Access Token

1. GitHub → Settings → Developer settings
2. Personal access tokens → Generate new token
3. Выберите права: `repo` (full control)
4. Generate token
5. **Скопируйте токен** (больше не увидите!)

Используйте URL с токеном:

```bash
GITHUB_REPO="https://ВАШ_ТОКЕН@github.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ.git" \
curl -fsSL https://... | sudo bash
```

---

## Настройка параметров автоустановки

Можете указать свои параметры через переменные окружения:

```bash
GITHUB_REPO="https://github.com/user/repo.git" \
GIT_BRANCH="main" \
APP_USER="myshop" \
DB_NAME="myshop_db" \
DB_USER="myshop_user" \
DB_PASSWORD="мой_пароль_123" \
APP_PORT="5000" \
LOAD_SEED_DATA="yes" \
sudo -E ./scripts/master_deploy.sh
```

### Доступные параметры:

| Параметр       | По умолчанию | Описание                |
| -------------- | ------------ | ----------------------- |
| GITHUB_REPO    | (пусто)      | URL репозитория         |
| GIT_BRANCH     | main         | Ветка Git               |
| APP_USER       | shopapp      | Пользователь приложения |
| DB_NAME        | shop_db      | Имя базы данных         |
| DB_USER        | shop_user    | Пользователь БД         |
| DB_PASSWORD    | (авто)       | Пароль БД               |
| APP_PORT       | 5000         | Порт приложения         |
| LOAD_SEED_DATA | (нет)        | yes для тестовых данных |

---

# Обновление приложения

После того как вы изменили код и запушили в GitHub:

## Шаг 1: На вашем компьютере

```bash
# Внесите изменения в код
# ...

# Сохраните изменения
git add .
git commit -m "Описание изменений"
git push
```

## Шаг 2: На VPS

```bash
# Подключитесь к VPS
ssh root@ВАШ_IP

# Перейдите в папку скриптов
cd /home/shopapp/app/scripts

# Запустите скрипт обновления
sudo ./update_vps.sh
```

### Что делает update_vps.sh:

1. ✅ Создаёт резервную копию базы данных
2. ✅ Обновляет код из GitHub
3. ✅ Устанавливает новые зависимости (если есть)
4. ✅ Собирает фронтенд заново
5. ✅ Перезапускает приложение

**Время: 2-3 минуты**

---

# Настройка домена и SSL

## Подключение своего домена

### Шаг 1: Настройте DNS

В панели управления вашего домена (Cloudflare, REG.RU, и т.д.):

1. Создайте A-запись:

   - **Тип**: A
   - **Имя**: @ (или ваш поддомен)
   - **Значение**: IP_АДРЕС_VPS
   - **TTL**: 300 (или Auto)

2. Подождите 5-60 минут (пока DNS обновится)

3. Проверьте командой:
   ```bash
   ping ваш-домен.com
   ```
   Должен показать IP вашего VPS

### Шаг 2: Настройте Nginx на VPS

```bash
# Подключитесь к VPS
ssh root@ВАШ_IP

# Запустите скрипт настройки домена
cd /home/shopapp/app
sudo ./setup_domain.sh
```

Скрипт спросит:

```
Введите ваш домен: ваш-домен.com
```

Готово! Теперь сайт доступен по домену.

---

## Установка SSL сертификата (HTTPS)

### Шаг 1: Убедитесь что домен настроен

```bash
ping ваш-домен.com
```

Должен показать IP вашего VPS.

### Шаг 2: Установите SSL

```bash
# На VPS
cd /home/shopapp/app
sudo ./setup_ssl.sh
```

Скрипт спросит:

```
Введите ваш домен: ваш-домен.com
Введите email для уведомлений: ваш@email.com
```

### Что происходит:

1. Устанавливается Certbot
2. Получается бесплатный SSL от Let's Encrypt
3. Настраивается автоматическое продление
4. Nginx перенастраивается на HTTPS

**Готово!** Теперь сайт доступен по `https://ваш-домен.com`

### Автоматическое продление

SSL сертификат действителен 90 дней и продлевается автоматически.

Проверить можно:

```bash
sudo certbot renew --dry-run
```

---

# Полезные команды

## Управление приложением

```bash
# Проверить статус
systemctl status shop-app ai-bot telegram-bot

# Перезапустить
systemctl restart shop-app ai-bot telegram-bot

# Посмотреть логи
journalctl -u shop-app -f
journalctl -u ai-bot -f
journalctl -u telegram-bot -f
```

## Управление Nginx

```bash
# Проверить статус
systemctl status nginx

# Перезапустить
systemctl restart nginx

# Проверить конфигурацию
nginx -t

# Логи Nginx
tail -f /var/log/nginx/shop_error.log
tail -f /var/log/nginx/shop_access.log
```

## Управление базой данных

```bash
# Подключиться к базе
sudo -u postgres psql shop_db

# Резервная копия
cd /home/shopapp/app
sudo ./backup_db.sh

# Восстановить из копии
sudo ./restore_db.sh

# Посмотреть все товары
sudo -u postgres psql shop_db -c "SELECT * FROM products;"

# Посмотреть все категории
sudo -u postgres psql shop_db -c "SELECT * FROM categories;"
```

## Посмотреть настройки

```bash
# Пароль базы данных и другие настройки
cat /home/shopapp/app/.env

# Конфигурация магазина
cat /home/shopapp/app/config/settings.json
```

## Исправить права доступа

Если видите белую страницу или ошибки 403:

```bash
cd /home/shopapp/app
sudo ./fix_permissions.sh
```

---

# Админ-панель

Админ-панель доступна по адресу: `http://ваш-сайт/admin`

## Первый вход в админку

### Способ 1: Автоматический (рекомендуется)

1. Зарегистрируйте аккаунт на сайте (обычная регистрация)
2. Перейдите на `/admin/login`
3. Введите email и пароль вашего аккаунта
4. Если в системе нет администраторов — вы автоматически станете первым админом (superadmin)

### Способ 2: Через базу данных

```bash
ssh root@ВАШ_IP
sudo -u postgres psql shop_db

-- Сделайте пользователя администратором
UPDATE users SET is_admin = TRUE, is_superadmin = TRUE WHERE email = 'ваш@email.com';

\q
```

## Возможности админ-панели

### Вкладка "Товары"

- **Просмотр** всех товаров с поиском и фильтрацией
- **Добавление** нового товара с загрузкой фото через Cloudinary
- **Редактирование** — изменение названия, описания, цены, категории
- **Удаление** товаров

При добавлении товара можно указать:

- Название и описание
- Цену
- Категорию
- До 5 изображений (загружаются через Cloudinary)
- Цвета (например: Красный, Синий, Зеленый)
- До 2 дополнительных атрибутов (например: Размер с вариантами S, M, L, XL)

### Вкладка "Категории"

- **Создание** новых категорий с иконкой (эмодзи)
- **Редактирование** названия и иконки
- **Удаление** категорий
- **Порядок сортировки** — указывается числом

### Вкладка "Заказы"

- **Просмотр** всех заказов с информацией о покупателе
- **Фильтрация** по статусу (ожидает, оплачен, отправлен, доставлен)
- **Изменение статуса** заказа
- **Просмотр чека** — если покупатель загрузил скриншот оплаты
- **Детали заказа** — товары, количество, адрес доставки

### Вкладка "Статистика"

- **Количество пользователей**
- **Общее количество заказов**
- **Выручка** (сумма оплаченных заказов)
- **Конверсия** — процент пользователей, сделавших заказ

### Вкладка "Настройки"

#### Cloudinary (для загрузки изображений)

```
Cloud Name: ваш_cloud_name
API Key: ваш_api_key
API Secret: ваш_api_secret
```

Получить данные можно на [cloudinary.com](https://cloudinary.com) (бесплатно).

#### Telegram уведомления

```
Bot Token: токен_бота_от_BotFather
Admin Chat ID: ваш_chat_id
Включить: Да/Нет
```

#### Платежные системы

Настройка Click, Payme, Uzum Bank, перевод на карту — прямо из интерфейса.

### Вкладка "Менеджеры" (только для superadmin)

- **Просмотр** списка администраторов
- **Добавление** нового администратора из существующих пользователей
- **Удаление** прав администратора

---

# Платежные системы

Система поддерживает несколько способов оплаты. Методы отображаются на сайте только если они настроены.

## Настройка через админ-панель

1. Войдите в `/admin`
2. Перейдите в "Настройки"
3. Настройте нужные платежные системы

## Перевод на карту

Самый простой способ приёма платежей.

### Настройка в `config/settings.json`:

```json
{
	"payment": {
		"cardTransfer": {
			"enabled": true,
			"cardNumber": "8600 1234 5678 9012",
			"cardHolder": "Иванов Иван Иванович",
			"bankName": "Uzcard"
		}
	}
}
```

### Как это работает:

1. Покупатель оформляет заказ
2. Видит реквизиты карты для перевода
3. Делает перевод через приложение банка
4. Загружает скриншот чека
5. Администратор видит чек в разделе "Заказы" и подтверждает оплату

## Click

Онлайн оплата для Узбекистана.

### Настройка:

1. Зарегистрируйтесь в [Click Merchant](https://merchant.click.uz)
2. Получите `Merchant ID` и `Service ID`
3. Создайте секретный ключ в кабинете

### В `config/settings.json`:

```json
{
	"payment": {
		"click": {
			"enabled": true,
			"merchantId": "12345",
			"serviceId": "67890"
		}
	}
}
```

### Переменные окружения (секретные):

```bash
CLICK_MERCHANT_ID=12345
CLICK_SERVICE_ID=67890
CLICK_SECRET_KEY=ваш_секретный_ключ
```

### Webhook URLs (укажите в кабинете Click):

```
Prepare: https://ваш-домен.com/api/webhooks/click/prepare
Complete: https://ваш-домен.com/api/webhooks/click/complete
```

## Payme

Онлайн оплата для Узбекистана.

### Настройка:

1. Зарегистрируйтесь в [Payme Business](https://business.payme.uz)
2. Получите `Merchant ID`
3. Создайте ключ для проверки запросов

### В `config/settings.json`:

```json
{
	"payment": {
		"payme": {
			"enabled": true,
			"merchantId": "your_merchant_id"
		}
	}
}
```

### Переменные окружения:

```bash
PAYME_MERCHANT_ID=your_merchant_id
PAYME_KEY=your_secret_key
```

### Webhook URL:

```
https://ваш-домен.com/api/webhooks/payme
```

## Uzum Bank

Онлайн оплата для Узбекистана.

### Настройка:

1. Подключитесь к Uzum Business
2. Получите `Merchant ID` и `Service ID`
3. Получите секретный ключ для HMAC подписи

### В `config/settings.json`:

```json
{
	"payment": {
		"uzum": {
			"enabled": true,
			"merchantId": "your_merchant_id",
			"serviceId": "your_service_id"
		}
	}
}
```

### Переменные окружения:

```bash
UZUM_MERCHANT_ID=your_merchant_id
UZUM_SERVICE_ID=your_service_id
UZUM_SECRET_KEY=your_secret_key
```

### Webhook URLs:

```
Check: https://ваш-домен.com/api/webhooks/uzum/check
Create: https://ваш-домен.com/api/webhooks/uzum/create
Confirm: https://ваш-домен.com/api/webhooks/uzum/confirm
Reverse: https://ваш-домен.com/api/webhooks/uzum/reverse
```

## Безопасность платежей

- **Секретные ключи** хранятся только в переменных окружения, никогда в коде или конфигах
- **Все webhook'и** проверяют подпись/авторизацию перед обработкой
- **Суммы заказов** вычисляются на сервере из цен в БД (не доверяем клиенту)
- **Данные шифруются** при хранении в БД с помощью Fernet

---

# Telegram уведомления

Система отправляет уведомления о новых заказах администратору в Telegram.

## Настройка

### Шаг 1: Создайте бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Введите имя бота (например: "My Shop Notifications")
4. Введите username бота (например: `myshop_notify_bot`)
5. Скопируйте токен бота

### Шаг 2: Узнайте ваш Chat ID

1. Напишите боту [@userinfobot](https://t.me/userinfobot) команду `/start`
2. Скопируйте ваш ID (число)

### Шаг 3: Настройте переменные окружения

```bash
# Добавьте в .env файл на VPS
TELEGRAM_BOT_TOKEN=123456789:AAHxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TELEGRAM_ADMIN_CHAT_ID=987654321
```

### Шаг 4: Включите уведомления

В `config/settings.json`:

```json
{
	"telegramNotifications": {
		"enabled": true,
		"adminChatId": "987654321"
	}
}
```

Или через админ-панель: Настройки → Telegram уведомления.

## Что содержит уведомление

При каждом новом заказе администратор получает сообщение:

- 🆔 Номер заказа
- 👤 Имя покупателя
- 📱 Телефон
- 📍 Адрес доставки
- 📦 Список товаров с количеством
- 💰 Общая сумма
- 💳 Способ оплаты
- 🧾 Скриншот чека (если перевод на карту)

---

# Email и сброс пароля

Система поддерживает сброс пароля через email.

## Как это работает

1. Пользователь нажимает "Забыли пароль?" на странице входа
2. Вводит свой email
3. Получает письмо со ссылкой для сброса
4. Переходит по ссылке и вводит новый пароль
5. Ссылка действительна 1 час и работает только один раз

## Настройка SMTP

### Через переменные окружения:

```bash
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your@gmail.com
SMTP_PASSWORD=your_app_password
SMTP_FROM_EMAIL=your@gmail.com
SMTP_FROM_NAME=Мой Магазин
SMTP_USE_TLS=true
```

### Через админ-панель:

Настройки → SMTP → Заполните все поля

### Настройка для Gmail:

1. Включите двухфакторную аутентификацию в Google аккаунте
2. Создайте пароль приложения:
   - Google Account → Security → App passwords
   - Создайте пароль для "Mail"
3. Используйте этот пароль в `SMTP_PASSWORD`

### Настройка для Yandex:

```bash
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_USE_TLS=false  # используется SSL
```

### Настройка для Mail.ru:

```bash
SMTP_HOST=smtp.mail.ru
SMTP_PORT=465
SMTP_USE_TLS=false  # используется SSL
```

## Проверка работы

После настройки:

1. Перейдите на `/forgot-password`
2. Введите email существующего пользователя
3. Проверьте почту (включая папку "Спам")

---

# Решение проблем

## Белая страница / Ошибки 403 Forbidden

### Проблема:

Сайт не загружается, в браузере белая страница.

### Решение:

```bash
# Исправьте права доступа
cd /home/shopapp/app
sudo ./fix_permissions.sh

# Перезапустите Nginx
systemctl restart nginx
```

Откройте сайт снова - должно заработать.

---

## Приложение не запускается

### Проверьте логи:

```bash
# Логи приложения
journalctl -u shop-app -n 100

# Статус
systemctl status shop-app
```

### Типичные ошибки:

**1. Ошибка подключения к БД:**

```bash
# Проверьте PostgreSQL
systemctl status postgresql

# Проверьте пароль в .env
cat /home/shopapp/app/.env
```

**2. Порт занят:**

```bash
# Проверьте какой процесс использует порт 5000
sudo lsof -i :5000

# Убейте процесс (замените PID)
kill -9 PID
```

**3. Не собран фронтенд:**

```bash
cd /home/shopapp/app
sudo -u shopapp npm run build
systemctl restart shop-app
```

---

## Git pull не работает

### Проблема:

`git pull` выдаёт ошибки или не обновляет код.

### Решение:

```bash
# Сбросить локальные изменения
cd /home/shopapp/app
sudo -u shopapp git stash

# Обновить
sudo -u shopapp git pull

# Или принудительно
sudo -u shopapp git fetch origin
sudo -u shopapp git reset --hard origin/main
```

---

## SSL сертификат не устанавливается

### Проблема:

Certbot выдаёт ошибку при установке SSL.

### Решение:

**1. Проверьте DNS:**

```bash
ping ваш-домен.com
```

Должен показывать IP вашего VPS.

**2. Проверьте порты 80 и 443:**

```bash
sudo ufw status
# Должно быть разрешено: 80/tcp, 443/tcp
```

**3. Попробуйте ещё раз:**

```bash
cd /home/shopapp/app
sudo ./setup_ssl.sh
```

---

## База данных не подключается

### Проверка:

```bash
# PostgreSQL запущен?
systemctl status postgresql

# Можно подключиться?
sudo -u postgres psql -c "SELECT 1"

# Проверьте DATABASE_URL
cat /home/shopapp/app/.env
```

### Решение:

```bash
# Перезапустите PostgreSQL
systemctl restart postgresql

# Проверьте пользователя БД
sudo -u postgres psql -c "\du"

# Пересоздайте пользователя если нужно
sudo -u postgres psql <<EOF
DROP USER IF EXISTS shop_user;
CREATE USER shop_user WITH PASSWORD 'ваш_пароль';
GRANT ALL PRIVILEGES ON DATABASE shop_db TO shop_user;
EOF
```

---

## Медленная работа

### Оптимизация:

```bash
# Увеличьте количество worker'ов
sudo nano /etc/systemd/system/shop-app.service

# Измените строку:
ExecStart=.../gunicorn app:app --bind 127.0.0.1:5000 --workers 8 --timeout 120

# Перезапустите
systemctl daemon-reload
systemctl restart shop-app
```

Количество workers: `2 × количество_CPU + 1`

---

## Очистка диска

Если место на диске заканчивается:

```bash
# Очистить логи
sudo journalctl --vacuum-time=7d

# Очистить старые пакеты
sudo apt autoremove
sudo apt clean

# Удалить старые резервные копии БД
cd /home/shopapp/app/backups
ls -lt
# Удалите старые .sql файлы
```

---

# Резервное копирование

## Автоматическая резервная копия БД

```bash
cd /home/shopapp/app
sudo ./backup_db.sh
```

Копия сохраняется в `backups/shop_db_YYYY-MM-DD_HH-MM-SS.sql`

## Восстановление из копии

```bash
cd /home/shopapp/app
sudo ./restore_db.sh
```

Скрипт покажет список доступных копий.

## Настройка автоматических копий

Создайте cron задачу:

```bash
sudo crontab -e

# Добавьте строку (копия каждый день в 3 ночи):
0 3 * * * /home/shopapp/app/backup_db.sh

# Сохраните и выйдите
```

---

# Мониторинг

## Проверка работоспособности

```bash
# Проверить что сайт отвечает
curl -I http://localhost:5000

# Должно быть: HTTP/1.1 200 OK

# Проверить использование ресурсов
htop

# Использование диска
df -h

# Размер директории приложения
du -sh /home/shopapp/app
```

---

# Telegram Bot (опционально)

Если хотите добавить Telegram бота для управления:

```bash
cd /home/shopapp/app/telegram_bot

# Установите зависимости
pip install -r requirements.txt

# Настройте бота
nano settingsbot.json

# Запустите
python3 bot.py
```

Подробнее в `telegram_bot/README.md`

---

# Заключение

## Чек-лист развертывания:

- [ ] Настроили `config/settings.json`
- [ ] Заменили логотип
- [ ] Закоммитили изменения в GitHub
- [ ] Запустили автоустановку на VPS
- [ ] Добавили товары в базу данных
- [ ] Проверили что сайт работает
- [ ] Настроили домен (опционально)
- [ ] Установили SSL (опционально)
- [ ] Настроили резервное копирование

## Полезные ссылки:

- **Автоустановка**: `auto_deploy.sh`
- **Интерактивная установка**: `deploy_vps.sh`
- **Обновление**: `update_vps.sh`
- **Резервная копия**: `backup_db.sh`
- **Исправление прав**: `fix_permissions.sh`
- **Настройка домена**: `setup_domain.sh`
- **Настройка SSL**: `setup_ssl.sh`

---

**Успешного запуска вашего магазина! 🚀**

Если что-то не работает - смотрите раздел "Решение проблем" выше.
