# 🚀 Быстрый старт - Развертывание на VPS

## 📋 Что вам нужно

- **VPS**: Ubuntu 22.04
- **IP**: YOUR_VPS_IP
- **Доступ**: SSH с правами root
- **Репозиторий**: GitHub (у вас уже есть!)

---

## ⚡ Самый быстрый способ - Мастер-скрипт (1 команда!)

### 1️⃣ Подключитесь к VPS

```bash
ssh root@YOUR_VPS_IP
```

### 2️⃣ Запустите мастер-установку

```bash
# Клонируйте и запустите всё одной командой
git clone https://github.com/ваш-username/ваш-репозиторий.git app
cd app/scripts
chmod +x master_deploy.sh
sudo ./master_deploy.sh
```

**Замените:**

- `ваш-username` - на ваш username в GitHub
- `ваш-репозиторий` - на название вашего репозитория

**Замените:**

- `ваш-username` - на ваш username в GitHub
- `ваш-репозиторий` - на название вашего репозитория

---

3️⃣ Настройка домена и SSL

```bash
# Настройте домен (Nginx)
sudo ./setup_domain.sh

# Получите бесплатный SSL
sudo ./setup_ssl.sh
```

**Готово!** Теперь ваш магазин доступен по HTTPS.

Вас попросят ввести:

- Пароль для базы данных (придумайте надежный)
- Остальное можно оставить по умолчанию (просто нажимайте Enter)

### ✅ Готово!

После установки откройте в браузере:

```
http://YOUR_VPS_IP
```

---

## 🔧 Основные команды после установки

```bash
# Проверить статус
systemctl status shop-app

# Перезапустить
systemctl restart shop-app

# Посмотреть логи
journalctl -u shop-app -f

# Обновить приложение
cd /home/shopapp/app/scripts
sudo ./update_vps.sh

# Сделать резервную копию БД
sudo ./backup_db.sh

# Восстановить из резервной копии
sudo ./restore_db.sh

# Исправить права доступа (если 403 ошибки)
sudo ./fix_permissions.sh

# Подключить свой домен
sudo ./setup_domain.sh

# Установить SSL (HTTPS)
sudo ./setup_ssl.sh
```

---

## 🔄 Обновление приложения из GitHub

Когда вы внесете изменения в код и запушите в GitHub:

```bash
# 1. На вашем компьютере - закоммитьте изменения
git add .
git commit -m "Описание ваших изменений"
git push

# 2. На VPS - обновите через скрипт
ssh root@YOUR_VPS_IP
cd /home/shopapp/app/scripts
sudo ./update_vps.sh
```

**Скрипт update_vps.sh автоматически:**

- ✅ Создаст резервную копию БД перед обновлением
- ✅ Обновит код из GitHub
- ✅ Установит новые зависимости (если есть)
- ✅ Соберет фронтенд заново
- ✅ Перезапустит приложение

---

## 📚 Подробная документация

Если что-то пошло не так или нужна детальная информация:

- **Полное руководство**: `ПОЛНОЕ_РУКОВОДСТВО.md` ⭐
- **Мастер-скрипт**: `SCRIPTS_GUIDE.md` 🛠️
- **Бот ассистент**: `AI_BOT_README.md` 🤖

---

## 💡 Что делает автоматический скрипт?

✅ Устанавливает PostgreSQL  
✅ Создает базу данных  
✅ Устанавливает Node.js и Python  
✅ Собирает фронтенд  
✅ Настраивает права доступа для Nginx  
✅ Настраивает systemd сервис  
✅ Настраивает Nginx  
✅ Настраивает Firewall  
✅ Загружает тестовые данные (по желанию)

---

## 🆘 Если что-то не работает

### Приложение не открывается в браузере

```bash
# Проверьте статус
systemctl status shop-app
systemctl status nginx

# Посмотрите логи
journalctl -u shop-app -n 50
tail -f /var/log/nginx/shop_error.log
```

### Белая страница / Ошибки 403 Forbidden

Если видите белую страницу и ошибки в консоли браузера (F12):

```bash
# Автоматическое исправление прав доступа
cd /home/shopapp/app
sudo ./fix_permissions.sh
```

Затем обновите страницу в браузере!

### База данных не подключается

```bash
# Проверьте PostgreSQL
systemctl status postgresql

# Проверьте настройки
cat /home/shopapp/app/.env
```

### Нужна помощь?

Откройте `VPS_DEPLOY_GUIDE.md` - там подробное решение всех проблем!

---

**Вопросы? Смотрите полную документацию в VPS_DEPLOY_GUIDE.md**
