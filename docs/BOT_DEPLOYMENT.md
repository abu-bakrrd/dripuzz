# 🤖 Развертывание Telegram Бота на VPS

## Проблема

Если вы пытаетесь запустить бота на Windows/Mac и видите ошибку:
```
❌ Ошибка подключения к БД: Connection timed out
connection to server at "81.162.55.47", port 5432 failed
```

Это означает, что **бот не может подключиться к PostgreSQL на VPS**, потому что база данных настроена только на локальные подключения (это сделано для безопасности).

## ✅ Правильное решение

**Telegram бот должен работать на том же VPS, где находится база данных.**

### Шаг 1: Подключитесь к VPS

```bash
ssh root@ВАШ_IP_VPS
```

### Шаг 2: Перейдите в папку проекта

```bash
cd /opt/telegram-shop
```

### Шаг 3: Запустите скрипт развертывания бота

```bash
chmod +x deploy_bot_to_vps.sh
sudo ./deploy_bot_to_vps.sh
```

Скрипт автоматически:
- ✅ Установит Python зависимости для бота
- ✅ Настроит `.env` файл с правильным `DATABASE_URL` (localhost)
- ✅ Создаст systemd сервис для автоматического запуска
- ✅ Запустит бота в фоновом режиме

### Шаг 4: Проверьте статус бота

```bash
sudo systemctl status telegram-bot
```

Должны увидеть:
```
● telegram-bot.service - Telegram Shop Bot
   Active: active (running)
```

### Шаг 5: Просмотр логов в реальном времени

```bash
sudo journalctl -u telegram-bot -f
```

Вы увидите:
```
🤖 Бот запущен и готов к работе...
👥 Авторизованных пользователей: 1
   IDs: [5644397480]
🔄 Подключение к Telegram...
```

## 🔧 Полезные команды

### Управление ботом

```bash
# Перезапустить бота
sudo systemctl restart telegram-bot

# Остановить бота
sudo systemctl stop telegram-bot

# Запустить бота
sudo systemctl start telegram-bot

# Статус бота
sudo systemctl status telegram-bot

# Посмотреть логи
sudo journalctl -u telegram-bot -f

# Посмотреть последние 100 строк логов
sudo journalctl -u telegram-bot -n 100
```

### Обновление бота

Если вы изменили код бота:

```bash
cd /opt/telegram-shop
git pull  # если используете Git
sudo systemctl restart telegram-bot
```

## 📝 Настройка .env файла

Файл находится в `/opt/telegram-shop/telegram_bot/.env`

```bash
# Редактировать .env
sudo nano /opt/telegram-shop/telegram_bot/.env
```

Пример содержимого:
```env
DATABASE_URL=postgresql://shopuser:PASSWORD@localhost:5432/shop_db
TELEGRAM_BOT_TOKEN=7845901234:AAHfG...
CLOUDINARY_CLOUD_NAME=your-cloud
CLOUDINARY_API_KEY=123456789
CLOUDINARY_API_SECRET=abc123xyz
```

**Важно:** `DATABASE_URL` должен использовать `@localhost`, а не IP адрес!

После изменений:
```bash
sudo systemctl restart telegram-bot
```

## ❓ Частые проблемы

### Бот не запускается

1. Проверьте логи:
```bash
sudo journalctl -u telegram-bot -n 50
```

2. Проверьте .env файл:
```bash
cat /opt/telegram-shop/telegram_bot/.env
```

3. Проверьте, что PostgreSQL работает:
```bash
sudo systemctl status postgresql
```

### Бот не отвечает

1. Проверьте авторизацию в `settingsbot.json`:
```bash
cat /opt/telegram-shop/telegram_bot/settingsbot.json
```

2. Убедитесь, что ваш Telegram ID добавлен в список `authorizedUsers`

### База данных недоступна

1. Проверьте, что PostgreSQL слушает на localhost:
```bash
sudo netstat -tlnp | grep 5432
```

Должно быть:
```
tcp  0  0  127.0.0.1:5432  0.0.0.0:*  LISTEN
```

## 🚀 Автозапуск

Бот автоматически:
- ✅ Запускается при загрузке VPS
- ✅ Перезапускается при сбоях (через 10 секунд)
- ✅ Переподключается при разрыве связи с Telegram

## 🔐 Безопасность

- База данных доступна только с localhost (безопасно)
- Все пароли и токены хранятся в .env файле
- Бот работает как systemd сервис с автоматическим перезапуском

---

## 💡 Почему бот должен работать на VPS?

1. **Безопасность:** PostgreSQL не открыт для внешних подключений
2. **Надежность:** Бот работает 24/7 без вашего компьютера
3. **Скорость:** Нет задержек сети между ботом и БД
4. **Простота:** Не нужно настраивать firewall и внешние подключения

**Это правильный и рекомендуемый способ развертывания!** ✅
