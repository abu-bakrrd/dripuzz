# 🚀 НАЧНИТЕ ЗДЕСЬ

## ⚡ Установка за 1 команду (БЕЗ ВОПРОСОВ!)

### Подключитесь к VPS:
```bash
ssh root@ВАШ_IP
```

### Запустите одну команду:
```bash
GITHUB_REPO="https://github.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ.git" \
curl -fsSL https://raw.githubusercontent.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ/main/auto_deploy.sh | sudo bash
```

**Замените:**
- `ВАШ_USERNAME` → ваш username в GitHub
- `ВАШ_РЕПОЗИТОРИЙ` → название репозитория

### ✅ Готово!
Через 3-5 минут откройте: **http://ВАШ_IP**

---

## 📝 Что происходит автоматически?

✅ Установка всех пакетов (PostgreSQL, Nginx, Node.js, Python)  
✅ Клонирование кода из GitHub  
✅ Создание базы данных с автоматическим паролем  
✅ Сборка фронтенда  
✅ Настройка и запуск приложения  

**Никаких вопросов - всё сделается само!**

---

## 🔐 Приватный репозиторий?

Сначала настройте SSH ключ:

```bash
# На VPS
ssh-keygen -t ed25519 -C "vps"
cat ~/.ssh/id_ed25519.pub

# Скопируйте ключ и добавьте в GitHub:
# Settings → SSH and GPG keys → New SSH key
```

Используйте SSH URL:
```bash
GITHUB_REPO="git@github.com:ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ.git" \
curl -fsSL https://raw.githubusercontent.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ/main/auto_deploy.sh | sudo bash
```

---

## 🔄 Обновление после изменений в коде

```bash
# На компьютере
git add .
git commit -m "Описание изменений"
git push

# На VPS
ssh root@ВАШ_IP
cd /home/shopapp/app
sudo -u shopapp git pull
sudo ./update_vps.sh
```

---

## 🛠️ Полезные команды

```bash
# Статус приложения
systemctl status shop-app

# Логи в реальном времени
journalctl -u shop-app -f

# Перезапустить приложение
systemctl restart shop-app

# Посмотреть пароль базы данных
cat /home/shopapp/app/.env

# Исправить ошибки 403
cd /home/shopapp/app && sudo ./fix_permissions.sh
```

---

## 📚 Дополнительная документация

- **Подробная инструкция**: `БЫСТРАЯ_УСТАНОВКА.md`
- **Интерактивная установка**: `QUICK_START_RU.md`
- **Настройка домена**: `DOMAIN_SETUP_RU.md`
- **Все скрипты**: `VPS_SCRIPTS_README.md`

---

## 💡 Два способа установки

### 1. Автоматическая (рекомендуется) ⚡
- **Скрипт**: `auto_deploy.sh`
- **Вопросы**: НЕТ
- **Время**: 3-5 минут
- **Сложность**: Просто скопировать команду

### 2. Интерактивная
- **Скрипт**: `deploy_vps.sh`
- **Вопросы**: ДА (можно настроить параметры)
- **Время**: 5-7 минут
- **Сложность**: Нужно вводить данные

**Выбирайте первый вариант для максимальной скорости!**

---

**Успешного развёртывания! 🎉**
