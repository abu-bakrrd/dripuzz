# 🖥️ Локальный запуск на Windows

Инструкция для запуска проекта на вашем компьютере (без VPS).

---

## ⚡ Быстрый старт

### Шаг 1: Установите необходимые программы

Если еще не установлены:

1. **Python 3.10+** → [python.org/downloads](https://www.python.org/downloads/)

   - При установке ✅ отметьте "Add Python to PATH"

2. **Node.js 18+** → [nodejs.org](https://nodejs.org/)

   - Скачайте LTS версию

3. **PostgreSQL** → [postgresql.org/download/windows](https://www.postgresql.org/download/windows/)
   - Запомните пароль, который укажете при установке!

---

### Шаг 2: Настройте базу данных

Откройте **PowerShell** или **CMD** и выполните:

```powershell
# Подключитесь к PostgreSQL (введите пароль, который указали при установке)
psql -U postgres

# В консоли PostgreSQL выполните:
CREATE DATABASE shop_db;
CREATE USER shop_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE shop_db TO shop_user;
\q
```

---

### Шаг 3: Создайте файл .env

В папке проекта `c:\MiniTaskerBot3` создайте файл `.env` со следующим содержимым:

```env
# База данных (замените your_password на ваш пароль)
DATABASE_URL=postgresql://shop_user:your_password@localhost:5432/shop_db

# Порт приложения
PORT=5000

# Режим разработки
FLASK_ENV=development

# Telegram Bot Token (если нужен бот)
# BOT_TOKEN=ваш_токен_от_BotFather

# Secret Key для Flask
SECRET_KEY=dev-secret-key-change-in-production
```

---

### Шаг 4: Установите зависимости

Откройте **PowerShell** или **CMD** в папке проекта:

```powershell
# Перейдите в папку проекта
cd c:\MiniTaskerBot3

# Установите Python зависимости
pip install -r requirements.txt

# Установите Node.js зависимости
npm install
```

---

### Шаг 5: Инициализируйте базу данных

```powershell
# Создайте таблицы в базе данных
python init_tables.py
```

---

### Шаг 6: Запустите проект

**Вариант 1: Запуск через скрипт (рекомендуется)**

Создайте файл `start_local.bat` в папке проекта:

```batch
@echo off
echo Запуск Flask сервера...
start cmd /k "python app.py"

timeout /t 3

echo Запуск Vite dev сервера...
start cmd /k "npm run dev"

echo.
echo ✅ Проект запущен!
echo.
echo 🌐 Откройте в браузере: http://localhost:5173
echo 📊 API доступен на: http://localhost:5000
echo.
pause
```

Затем просто запустите `start_local.bat` двойным кликом.

**Вариант 2: Ручной запуск**

Откройте **два окна PowerShell/CMD**:

**Окно 1 - Flask сервер:**

```powershell
cd c:\MiniTaskerBot3
python app.py
```

**Окно 2 - Vite dev сервер:**

```powershell
cd c:\MiniTaskerBot3
npm run dev
```

---

### Шаг 7: Откройте в браузере

Перейдите на: **http://localhost:5173**

Для админ-панели: **http://localhost:5173/admin**

---

## 🎯 Что дальше?

### Первый вход в админку:

1. Зарегистрируйтесь на сайте
2. Перейдите на `/admin/login`
3. Вы автоматически станете администратором (первый пользователь)

### Настройте магазин:

Отредактируйте `config/settings.json` под ваши нужды

### Добавьте товары:

Через админ-панель `/admin` → Товары → Добавить товар

---

## 🔧 Полезные команды

```powershell
# Остановить все процессы Python
taskkill /F /IM python.exe

# Остановить все процессы Node
taskkill /F /IM node.exe

# Пересоздать базу данных (УДАЛИТ ВСЕ ДАННЫЕ!)
psql -U postgres -c "DROP DATABASE shop_db;"
psql -U postgres -c "CREATE DATABASE shop_db;"
python init_tables.py
```

---

## ❓ Частые проблемы

### "psql не является внутренней командой"

PostgreSQL не добавлен в PATH. Используйте SQL Shell (psql) из меню Пуск.

### "pip не является внутренней командой"

Python не добавлен в PATH. Переустановите Python с галочкой "Add to PATH".

### "Ошибка подключения к базе данных"

Проверьте:

- PostgreSQL запущен (Services → postgresql)
- Пароль в `.env` совпадает с паролем PostgreSQL
- База данных `shop_db` создана

### "Port 5000 already in use"

Порт занят. Измените PORT в `.env` на другой (например, 5001).

### "npm install выдает ошибки"

Попробуйте:

```powershell
npm cache clean --force
npm install
```

---

## 🚀 Готово!

Ваш проект запущен локально!

- **Фронтенд**: http://localhost:5173
- **API**: http://localhost:5000
- **Админка**: http://localhost:5173/admin

**Успешной разработки! 💪**
