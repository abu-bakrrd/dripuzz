# 🛠 Руководство по скриптам (Scripts Guide)

В папке `scripts/` находятся инструменты для установки, обслуживания и управления проектом.

## 🚀 Установка и Развертывание

| Скрипт             | Описание                                                                      | Запуск                            |
| ------------------ | ----------------------------------------------------------------------------- | --------------------------------- |
| `deploy_vps.sh`    | **Главный установщик.** Ставит всё (Сайт, Бот, Базу) с нуля.                  | `sudo ./scripts/deploy_vps.sh`    |
| `deploy_ai_bot.sh` | **Обновление бота.** Обновляет код и перезапускает только AI Bot.             | `sudo ./scripts/deploy_ai_bot.sh` |
| `uninstall_vps.sh` | **Удаление.** Полностью удаляет проект с сервера.                             | `sudo ./scripts/uninstall_vps.sh` |
| `update_vps.sh`    | **Обновление сайта.** Обновляет код осного сайта (Flask) и перезапускает его. | `sudo ./scripts/update_vps.sh`    |

## 🗄 База Данных

| Скрипт           | Описание                                                   | Запуск                                |
| ---------------- | ---------------------------------------------------------- | ------------------------------------- |
| `backup_db.sh`   | **Бэкап.** Создает резервную копию базы данных в архив.    | `sudo ./scripts/backup_db.sh`         |
| `restore_db.sh`  | **Восстановление.** Восстанавливает БД из файла бэкапа.    | `sudo ./scripts/restore_db.sh <file>` |
| `init_tables.py` | **Инициализация.** Создает таблицы, если их нет.           | `python3 scripts/init_tables.py`      |
| `seed_db.py`     | **Тестовые данные.** Наполняет магазин тестовыми товарами. | `python3 scripts/seed_db.py`          |

## ⚙️ Настройка и Обслуживание

| Скрипт                | Описание                                                         | Запуск                               |
| --------------------- | ---------------------------------------------------------------- | ------------------------------------ |
| `setup_domain.sh`     | **Домен.** Привязывает ваш домен к серверу (Nginx).              | `sudo ./scripts/setup_domain.sh`     |
| `setup_ssl.sh`        | **SSL.** Получает бесплатный HTTPS сертификат (Let's Encrypt).   | `sudo ./scripts/setup_ssl.sh`        |
| `fix_permissions.sh`  | **Права доступа.** Чинит ошибки 403 Forbidden.                   | `sudo ./scripts/fix_permissions.sh`  |
| `enable_remote_db.sh` | **Удаленный доступ.** Открывает порт для подключения к БД извне. | `sudo ./scripts/enable_remote_db.sh` |
| `verify_vps.sh`       | **Диагностика.** Проверяет статус всех сервисов.                 | `sudo ./scripts/verify_vps.sh`       |

---

> **Примечание:** Все `.sh` скрипты на сервере нужно запускать с правами суперпользователя (`sudo`), предварительно дав разрешение на выполнение:
> `chmod +x scripts/*.sh`
