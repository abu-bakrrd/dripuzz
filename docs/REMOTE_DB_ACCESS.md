# 🌐 Настройка удаленного доступа к PostgreSQL

## Проблема

При запуске бота на Windows/Mac появляется ошибка:
```
❌ Ошибка подключения к БД: Connection timed out
```

Это происходит потому, что PostgreSQL на VPS настроен только на локальные подключения.

## ✅ Решение: Разрешить удаленные подключения

### Шаг 1: Настройте PostgreSQL на VPS

Подключитесь к VPS:
```bash
ssh root@ВАШ_IP_VPS
```

Перейдите в папку проекта:
```bash
cd /opt/telegram-shop
```

Запустите скрипт настройки:
```bash
chmod +x enable_remote_db.sh
sudo ./enable_remote_db.sh
```

Скрипт автоматически:
- ✅ Настроит PostgreSQL для прослушивания всех адресов
- ✅ Разрешит внешние подключения в `pg_hba.conf`
- ✅ Откроет порт 5432 в firewall
- ✅ Покажет вашу строку подключения

### Шаг 2: Создайте .env файл на вашем компьютере

В папке `telegram_bot/` создайте файл `.env`:

```env
DATABASE_URL=postgresql://shop_user:ВАШ_ПАРОЛЬ@81.162.55.47:5432/shop_db
TELEGRAM_BOT_TOKEN=7845901234:AAHfG...
CLOUDINARY_CLOUD_NAME=your-cloud
CLOUDINARY_API_KEY=123456789
CLOUDINARY_API_SECRET=abc123xyz
```

**Важно:** 
- Замените `ВАШ_ПАРОЛЬ` на реальный пароль от БД (тот, что вводили при развертывании)
- Замените `81.162.55.47` на ваш IP адрес VPS
- Укажите правильные данные Telegram бота и Cloudinary

### Шаг 3: Установите зависимости

В папке `telegram_bot/`:

```bash
pip install -r requirements.txt
```

Или если используете Python 3:
```bash
pip3 install -r requirements.txt
```

### Шаг 4: Запустите бота

```bash
python bot.py
```

Или:
```bash
python3 bot.py
```

Вы должны увидеть:
```
🤖 Бот запущен и готов к работе...
👥 Авторизованных пользователей: 1
   IDs: [5644397480]
🔄 Подключение к Telegram...
```

## 🧪 Проверка подключения к БД

### Тест 1: psql (если установлен)

```bash
psql "postgresql://shop_user:ПАРОЛЬ@81.162.55.47:5432/shop_db"
```

### Тест 2: Python скрипт

Создайте файл `test_db.py`:

```python
import psycopg2
import os
from dotenv import load_dotenv

load_dotenv()

try:
    conn = psycopg2.connect(os.environ.get('DATABASE_URL'))
    print("✅ Подключение к БД успешно!")
    cur = conn.cursor()
    cur.execute("SELECT COUNT(*) FROM products")
    count = cur.fetchone()[0]
    print(f"📦 Товаров в БД: {count}")
    conn.close()
except Exception as e:
    print(f"❌ Ошибка: {e}")
```

Запустите:
```bash
python test_db.py
```

## ⚠️ Безопасность

### Обязательно:

1. **Используйте сильный пароль БД** (минимум 16 символов, буквы+цифры+символы)

2. **Ограничьте доступ только с вашего IP** (опционально, но рекомендуется):

На VPS выполните:
```bash
# Узнайте ваш внешний IP
echo "Ваш IP:"
curl ifconfig.me
echo ""

# Ограничьте доступ только с этого IP
sudo nano /etc/postgresql/14/main/pg_hba.conf
```

Замените строку:
```
host    all             all             0.0.0.0/0               md5
```

На:
```
host    all             all             ВАШ_IP/32               md5
```

Перезапустите PostgreSQL:
```bash
sudo systemctl restart postgresql
```

3. **Регулярно обновляйте систему:**
```bash
sudo apt update && sudo apt upgrade -y
```

## 🔧 Альтернатива: SSH туннель (безопаснее)

Вместо открытия порта 5432, используйте SSH туннель:

```bash
# На Windows (PowerShell или cmd)
ssh -L 5432:localhost:5432 root@81.162.55.47

# Оставьте это окно открытым
```

Тогда в `.env` используйте:
```env
DATABASE_URL=postgresql://shop_user:ПАРОЛЬ@localhost:5432/shop_db
```

**Плюсы:** Безопаснее, не нужно открывать порт  
**Минусы:** Нужно держать SSH подключение активным

## ❓ Частые проблемы

### Connection timeout

**Причина:** Firewall блокирует порт

**Решение:**
```bash
# На VPS
sudo ufw allow 5432/tcp
sudo ufw reload
```

### Connection refused

**Причина:** PostgreSQL не слушает внешние адреса

**Решение:** Проверьте `postgresql.conf`:
```bash
sudo grep listen_addresses /etc/postgresql/14/main/postgresql.conf
```

Должно быть:
```
listen_addresses = '*'
```

### Authentication failed

**Причина:** Неправильный пароль

**Решение:** Сбросьте пароль на VPS:
```bash
sudo -u postgres psql
ALTER USER shop_user WITH PASSWORD 'новый_пароль';
\q
```

## 📊 Сравнение методов

| Метод | Безопасность | Сложность | Рекомендация |
|-------|--------------|-----------|--------------|
| **Бот на VPS** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ✅ Рекомендуется |
| **SSH туннель** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ✅ Хороший вариант |
| **Открытый порт** | ⭐⭐ | ⭐⭐ | ⚠️ Только для разработки |

---

**Итог:** Для разработки можно использовать удаленное подключение, но для продакшена лучше запускать бота на самом VPS.
